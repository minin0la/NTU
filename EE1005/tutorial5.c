#include <stdio.h>
int main(void)
{
    double salary, sales, basic;
    if (sales > 10000.0)
    {
        salary = basic + sales * 0.02;
    }
    else
    {
        salary = basic + sales * 0.01;
    }
    return 0;
    int x = 3;
    if (x == 4)
    {
        printf("What");
    }
}
